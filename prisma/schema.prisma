// This file is intentionally empty.
// The schema has been split into prisma/schema/*.prisma files.
// Prisma reads from the prisma/schema/ folder (configured in prisma.config.ts).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}